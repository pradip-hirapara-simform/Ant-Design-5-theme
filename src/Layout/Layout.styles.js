import Layout from "antd/es/layout/layout";
import styled from "styled-components";

export const PageWrapper = styled(Layout)`
 position: relative;
 `
export const PageBodyWrapper = styled(Layout)` 
 padding:100px 40px 0;
 min-height: 100vh;
`