import styled from "styled-components";

export const BlogCardTitle = styled.h3`
  font-size: 20px;
  line-height: 28px;
  color: ${(props) => props.theme.BlackTextColor};
  margin: 0 0 15px;
`;
export const BlogCardInfo = styled.p`
  font-size: 16px;
  line-height: 22px;
  color: ${(props) => props.theme.BlackTextColor};
  margin: 0;
`;
